> [!IMPORTANT] 
> This is for a school project, I don't plan to update it, but if you really want add more onto it for some reason, you can. 

It's svelte, using vite, so it's simple asl

`git clone https://github.com/addy10s/open-source-presentation.git`

`cd open-source-presentation`

`npm install`

`npm dev`